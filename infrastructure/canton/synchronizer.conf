canton {
  domains {
    mydomain {
      storage {
        type = memory  # In-memory for demo (H2 database)
      }
      
      public-api {
        address = "0.0.0.0"
        port = 5018
      }
      
      admin-api {
        address = "0.0.0.0"
        port = 5019
      }
    }
  }
}

