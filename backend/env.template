# Participant Ledger API URLs (Docker network - use container hostnames)
PARTICIPANT1_LEDGER_API=http://canton-participant1:5011
PARTICIPANT2_LEDGER_API=http://canton-participant2:5021
PARTICIPANT3_LEDGER_API=http://canton-participant3:5031

# Party IDs (from infrastructure/canton/party-ids.json)
# These are REAL Canton party IDs with cryptographic fingerprints
# Copy these from infrastructure/canton/party-ids.json after running init-canton-final.ps1
TECHBANK_PARTY_ID=TechBank::1220f8135b39957c1ce3344cc391b41b657be795756db9e6c3e63f5961c484b919ec
GLOBALCORP_PARTY_ID=GlobalCorp::12200e9f82e9a2d06ef6fa10b5785eccd3251eb9fca0f0a23a3c6ea644f6c284e8e7
RETAILFINANCE_PARTY_ID=RetailFinance::122063e334410bd5b0487acd7ceb0874c0c446772d1d5d14406332e2f82e4cbed809

# Server configuration
PORT=3001

# ⚠️ SECURITY: JWT Secret Configuration
# For production, ALWAYS generate a unique random secret:
#   Windows PowerShell: 
#     $bytes = [byte[]]::new(32); (New-Object Security.Cryptography.RNGCryptoServiceProvider).GetBytes($bytes); [Convert]::ToBase64String($bytes)
#   Linux/macOS: 
#     openssl rand -base64 32
#
# NEVER use this demo secret in production!
JWT_SECRET=demo-secret-key-canton-privacy-visualizer
JWT_EXPIRES_IN=24h

# ⚠️ SECURITY: CORS Configuration
# For production, ALWAYS set this to your frontend URL:
#   CORS_ORIGIN=https://your-frontend-domain.com
# 
# For development, use '*' (all origins allowed)
CORS_ORIGIN=*

# API Configuration (optional)
# API_DEFAULT_LIMIT=50
# API_MAX_LIMIT=200

# SSE Configuration (optional)
# SSE_MAX_CLIENTS=100
# SSE_KEEPALIVE_MS=30000

# Idempotency Configuration (optional)
# IDEMPOTENCY_CACHE_TTL_MS=300000

# Security Configuration (optional)
# REQUEST_SIZE_LIMIT=1mb

# Canton Configuration (optional)
# CANTON_LEDGER_ID=participant1

